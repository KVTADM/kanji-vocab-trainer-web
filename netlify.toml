# netlify.toml — KVT web (kanji-vocab-trainer-web)
#
# Structure du site depuis le 27/07/2026 :
#   /                       accueil : la communauté, puis « L'idée du projet »
#                           en section de bas de page (ancre #l-idee)
#   /app                    l'application elle-même
#   /a-propos /faq /blog/*  pages éditoriales, vrais dossiers
#   /downloads/*            binaires Mac et Windows
#
# Principe Netlify : un fichier statique existant est TOUJOURS servi avant une
# règle non forcée, et la première règle qui correspond gagne. L'ordre compte.

# 1) Téléchargements : le vrai fichier, sinon un 404 franc. Sans cette règle en
#    premier, un binaire manquant retomberait sur une page HTML et l'utilisateur
#    téléchargerait un faux fichier (bug rencontré le 23/07).
[[redirects]]
  from = "/downloads/*"
  to = "/404.html"
  status = 404

# 2) Anciennes adresses de la présentation. Elle n'est plus une page mais une
#    section de l'accueil : on redirige vers son ancre pour ne pas casser les
#    liens déjà partagés.
[[redirects]]
  from = "/decouvrir"
  to = "/#l-idee"
  status = 301

[[redirects]]
  from = "/decouvrir/*"
  to = "/#l-idee"
  status = 301

# 3) Filet pour l'app. Elle n'a aucun routage côté client aujourd'hui (aucun
#    pushState), donc cette règle ne sert qu'à éviter un 404 sur une sous-adresse
#    tapée à la main. À conserver si un vrai routage arrive un jour.
[[redirects]]
  from = "/app/*"
  to = "/app/index.html"
  status = 200

# Volontairement PAS de catch-all "/* -> /index.html" : une adresse inconnue
# doit donner un vrai 404, pas la page de présentation. Un catch-all masquerait
# les liens cassés au lieu de les révéler.
