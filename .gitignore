.DS_Store
.netlify/
*.log
# Les installeurs ont quitte ce dossier le 02/08/2026 (voir ../KVT-binaires).
# On garde la regle : si l'un d'eux revient ici par erreur, il ne partira
# au moins pas dans le depot.
downloads/*.dmg
downloads/*.exe

# Pages generees a chaque deploiement depuis la base (outils/generer-pages-deck.js).
# Les versionner ferait un commit de bruit a chaque avis poste, et la version
# du depot serait de toute facon perimee : la verite est en base.
/deck/
/sitemap.xml

# Genere par deploy.sh juste avant chaque publication (date du deploiement,
# affichee sur l'Accueil pour verifier qu'un deploiement a bien pris - voir
# app.js). Change a chaque fois, pas utile en historique git.
/version.json
